[project]
name = "pass-cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "qrcode[pil]>=8.0",
    "streamlit>=1.43.1",
]
